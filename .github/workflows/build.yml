name: Build
on: [push]

jobs:
  build:
    name: Build
    steps:
      - name: Build
        uses: rstudio/shinycannon-build-action@master
        with:
          outdir: 'out'
      - name: Show
        run: ls out
